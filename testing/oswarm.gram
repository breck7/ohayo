tooling onsave jtree build produceOSwarmGrammar
oswarmNode
 description The Swarm Language modified to specifically test Ohayo.
 root
 extends swarmNode
 javascript
  processArranged(arrangedInstance) {
   return arrangedInstance.getCommander()
  }
  _getProjectRootDir() {
   return this.isNodeJs() ? jtree.Utils.findProjectRoot(__dirname, "ohayo") : ""
  }
  getArrangeNode() {
   return {
    isAsync: () => true,
    arrange: () => {
     const OhayoWebApp = require(this._getProjectRootDir() + "ohayoWebApp/treeComponents/OhayoWebApp.js")
     const app = new OhayoWebApp()
     app.setVisitCount(2)
     app.renderApp()
     return app
    }
   }
  }